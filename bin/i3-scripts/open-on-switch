#!/bin/zsh
# open a workspace's window when we switch to it
# proper usage: open-on-switch [application] [workspace]

case $1 in
	"spotify")
		i3-msg "workspace $2"
		ps -elf | grep /usr/bin/spotify | grep -v grep
		if [ $? -eq 1 ]; then
			spotify
		fi
esac
