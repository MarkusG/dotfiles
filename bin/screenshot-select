#!/bin/zsh
# screenshot a region using maim and upload to ptpb.pw

pb() {
	curl -F c=@- 'https://ptpb.pw/?u=1'
}

echo '' | xsel -b
link="$(maim -s | pb).png"
echo $link | xsel -b
notify-send -t 1000 'Screenshot uploaded.' -i '/usr/share/icons/Arc/apps/symbolic/applets-screenshooter-symbolic.svg'
